// Package nodesocket has no way for a gateway to dial a node, on purpose: an inbound-only gateway
// cannot be turned into a client of every laptop in the fleet by a later refactor.
package nodesocket
