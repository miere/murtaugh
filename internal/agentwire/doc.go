// Package agentwire is the protocol a runtime node speaks to the gateway. It imports only
// internal/agent and internal/providerfail of ours so the gateway can use it without backends.
package agentwire
