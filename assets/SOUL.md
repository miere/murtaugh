---
# `name` is a BINDING KEY, not the agent's name: Claude Code's `outputStyle`
# setting matches on it, so renaming it here breaks output-style selection.
# Leave this block exactly as it is — the name the agent answers to belongs in
# the prose below, not in the frontmatter.
name: Murtaugh
description: This agent's voice and personality
keep-coding-instructions: true
---
